<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Caregiver Profile</title>
  <style>
    .button {
      display: inline-block;
      padding: 0.5em 1em;
      background-color: #007bff;
      color: white;
      text-decoration: none;
      border-radius: 5px;
    }

    .button:hover {
      background-color: #0056b3;
    }
    
  </style>
    <link rel="stylesheet" href="/styles.css">

</head>

<body>
  <h1>Caregiver Profile</h1>

  <section>
    <h2>My Info</h2>
    <p><strong>Name:</strong>
      <%= caregiver.contact_name || 'N/A' %>
    </p>
    <p><strong>Username:</strong>
      <%= caregiver.c_username || 'N/A' %>
    </p>
    <p><strong>Phone:</strong>
      <%= caregiver.contact_phone || 'N/A' %>
    </p>
    <p><strong>Email:</strong>
      <%= caregiver.contact_email || 'N/A' %>
    </p>
    <p><strong>Address:</strong>
      <%= caregiver.address || 'N/A' %>
    </p>
  </section>

  <!-- <section>
    <h2>Linked Elderly</h2>
    <% if (!elderlies || elderlies.length === 0) { %>
      <p>No elderly linked to you.</p>
    <% } else { %>
      <table border="1" cellpadding="5" cellspacing="0">
        <thead>
          <tr>
            <th>Name</th>
            <th>Username</th>
            <th>Contact</th>
            <th>Address</th>
            <th>Preferred Language</th>
            <th>Condition</th>
          </tr>
        </thead>
        <tbody>
          <% elderlies.forEach(e => { %>
            <tr>
              <td><%= e.name || 'N/A' %></td>
              <td><%= e.u_elderly_username || 'N/A' %></td>
              <td><%= e.contact || 'N/A' %></td>
              <td><%= e.address || 'N/A' %></td>
              <td><%= e.language_preference || 'N/A' %></td>
              <td><%= e.condition_special_consideration || 'N/A' %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    <% } %>
  </section> */
-->

  <section>
    <h2>Linked Elderly</h2>
    <p>
      Linking is not working due to reference fields, so you can see all elderlies here
    </p>
    <a href="/caregiver" class="button">View All Elderly</a>
  </section>
</body>

</html>